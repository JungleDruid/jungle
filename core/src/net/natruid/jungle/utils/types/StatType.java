package net.natruid.jungle.utils.types;

public enum StatType {
    HP, SPEED, DAMAGE, HEAL, ACCURACY, DODGE, AREA, DURATION, RANGE, AP, STUN;

    public static int length = StatType.values().length;
}
